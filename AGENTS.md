# PhotoRevive 项目安全规则

这些规则适用于在本仓库中工作的所有 Codex 任务。用户不是专业开发者，因此 Git、分支同步和发布安全由 Codex 主动负责，并用简明中文说明结果。

## 用户的实际工作方式

- 用户会同时打开多个 Codex 窗口持续开发，积累到一个阶段后，再新建一个“验收/发布窗口”统一检查、提交、推送和部署。
- 用户不需要判断哪些文件应该提交，也不需要设计 commit；验收窗口必须自行审阅全部差异、识别敏感文件和生成文件，并按功能逻辑整理提交。
- 普通开发窗口只负责代码、素材、文档和本地测试；默认不执行 commit、push、PR、合并或部署。
- 多个窗口如果都选择 Local，会共享同一个工作目录。`.codex/release.lock` 是“代码验收锁”：任何其他窗口看到它后，必须立即停止本仓库文件写入和 Git 状态修改，只允许做只读检查，并告诉用户“代码验收冻结中”。
- 与本次发布无关的 CMS 日常内容操作（例如通过现有 CMS 上传图片/视频、编辑文案或发布内容）不受代码验收锁限制，但必须由用户明确指定本次内容操作，且不得顺带修改仓库代码、CMS 程序、数据库结构、线上配置或执行部署。若内容操作会影响本次正在验收的同一素材、数据或接口，必须暂停并报告冲突。
- 对彼此独立、需要真正并行的开发任务，优先建议用户在新任务输入框下选择 Worktree；不得假设其他窗口的改动属于当前任务，也不得擅自合并其他 worktree。

## 开始任务前（强制）

1. 在修改任何文件前，先检查 `.codex/release.lock`，再运行 `git status --short --branch`，确认当前分支和未提交改动。
   - 如果锁存在且当前任务不是创建该锁的验收任务，停止本仓库文件写入和 Git 操作，不得删除锁；与本次发布无关的 CMS 日常内容操作按上面的代码验收锁例外执行。
2. 网络可用时运行 `git fetch origin --prune`，只更新远程引用；不要因此覆盖本地文件。
3. 禁止直接在 `main` 或 `master` 上开发：
   - 工作区干净时，从最新的 `origin/main` 创建 `codex/<简短任务名>` 分支后再修改。
   - 工作区不干净时，禁止 pull、rebase、reset、checkout 覆盖、自动 stash 或删除文件；先从当前 HEAD 创建新的 `codex/<简短任务名>` 分支，让现有改动原样留在新分支，再继续。
4. 如果已经处于合适的 `codex/*` 功能分支，保留并继续使用它；不要为了形式反复切分支。
5. 发现 `origin/main` 已更新时，先说明本分支落后情况。工作区或提交不干净时，不得自动 rebase/merge。

## 开发和合并规则

- `main` 是受保护的线上基线，只能通过 Pull Request 合并。
- 禁止直接向 `main`/`master` commit、push、force-push、merge、rebase 或 cherry-pick。
- 禁止使用 `--no-verify`、临时关闭 hooks、修改保护规则等方式绕过本仓库的安全措施。
- 普通开发窗口不得 commit 或 push。只有验收窗口在创建 `.codex/release.lock`、完成改动审计后才可以提交。
- 准备 PR 前必须再次 `git fetch origin --prune`，把最新 `origin/main` 安全整合到功能分支，解决冲突并运行相关测试。不得为了消除冲突而丢弃用户改动。
- 合并 PR 前，向用户报告：分支名、主要改动、测试结果、是否包含 `supabase/` 后端或数据库改动，以及与 `origin/main` 的同步状态。

## 验收/发布窗口流程（强制）

当用户说“验收”“准备发布”“帮我提交推送部署”或同等意思时，进入验收模式：

1. 先创建 `.codex/release.lock`，明确告诉用户：请暂停其他仍在修改本项目代码或 Git 状态的窗口；与本次发布无关的 CMS 日常内容操作可以继续。没有确认其他代码窗口已经暂停前，不得开始 commit、同步 main 或部署。
2. 把当前整个工作区视为“候选发布批次”。不要反问非技术用户“要提交哪些文件”，而是自行完成：
   - 查看 tracked、untracked、deleted 文件和完整 diff；
   - 按 App、测试、文档、素材、Supabase functions、database migrations 分类；
   - 排除密钥、`.env`、Supabase 临时连接文件、构建缓存、调试输出和无关生成物；
   - 检查是否有改到一半、明显冲突、重复实现或不属于本批次的内容。
3. 禁止一开始直接使用 `git add .` 或 `git add -A`。先列出计划提交的分组，再按明确路径分批暂存。功能强耦合时可以合成一个阶段性 commit，不要求用户理解拆分细节。
4. 提交前运行适当测试或构建；发现失败时先修复或报告，不得为了“先发上去”跳过失败。
5. 建立安全的本地提交后，再 `git fetch origin --prune`，检查本分支与最新 `origin/main` 的差异，在功能分支上完成整合并再次测试。
6. 用户明确说“提交并推送”或“提交推送部署”，表示允许验收窗口完成本地 commit、推送功能分支并创建 PR；但不等于允许直接推 main、合并 PR 或部署生产环境。
7. 创建 PR 后先给用户一份非技术验收摘要。必须分成两个独立关卡：
   - 关卡一：询问是否把该 PR 合并到 `main`；
   - 关卡二：合并完成后，列出将部署的 Supabase functions、migrations 或其他生产资源以及完整 main SHA，然后停止并等待用户发送新的明确部署命令。
8. 发布完成、取消或确认暂缓后，删除 `.codex/release.lock`，并报告仍未提交或未发布的内容。若发现锁可能属于另一个仍在进行的验收任务，不得擅自删除。

## 后端代码与部署边界（不可跳过）

- 本仓库只负责 iOS App。PhotoRevive 的 CMS 和生产 Supabase 后端唯一代码来源是 `/Users/mayingkun/mkwork/IOSreactive_ad_app/AIVideoAiApiCms` 及其 GitHub 仓库 `dahai827/AIVideoAiApiCms`。
- 本仓库中的本地 `supabase/` 是开发副本，已从 Git 跟踪中排除；发现后端改动时必须先逐项整合到主服务端仓库，禁止整目录复制覆盖。
- 禁止从本仓库执行任何 Supabase function、database migration、secret、link/unlink 或生产数据库操作；本仓库不提供部署脚本，也不得临时创建绕过入口。
- 用户说“提交推送部署”时，本仓库最多完成 iOS 功能分支 commit、push 和 PR。iOS `main` 合并、App Store/TestFlight 发布都必须单独展示影响并再次确认。
- 服务端合并与部署必须切换到主服务端仓库，并遵守该仓库的 `AGENTS.md`、最新 main SHA 和一次性部署授权门禁。

## 后端和生产环境红线

以下操作都视为生产操作，必须在执行前暂停，用非技术语言列出影响范围，并获得用户对“本次具体操作”的明确确认：

- 任何 Supabase 函数部署、数据库 push、远程 migration、secret 修改、项目 link/unlink。
- 任何生产数据库写入、删除、结构变更或数据修复。
- 任何发布、部署、上传线上资源、修改线上环境变量或第三方生产配置。
- 合并到 `main`、直接推送受保护分支、发布 App Store/TestFlight 构建。

通过现有 CMS 进行日常图片/视频上传、文案编辑或内容发布仍属于线上内容操作，需要用户明确说明本次要操作的内容；但它不等于代码部署，不要求用户发送“部署”，也不会因为代码验收锁而自动禁止。CMS 程序代码、Vercel、数据库、存储规则、密钥、批量删除或不可逆内容操作不属于该例外。

“把功能做好”“处理完”“你看着办”等宽泛表达不等于生产操作授权。没有明确确认时，只完成本地代码和测试，并在结尾明确写“未部署、未合并 main”。

即使用户一开始说“提交推送部署”，也只能直接进入验收流程并完成 commit、功能分支 push 和 PR；合并 `main` 与生产部署仍要分别展示影响并再次确认。生产部署确认必须是合并完成、部署清单展示之后的一条新用户消息，且消息中必须包含“部署”。

## Supabase 目录

- 本仓库中的 `supabase/` 不参与提交、PR 或部署；需要保留的改动必须人工审阅后合入主服务端仓库。
- 不得把 `.env`、访问令牌、service role key、数据库密码或其他密钥提交进 Git。

## 任务结束时

- 运行与改动相匹配的测试或构建；不能运行时说明原因。
- 最终回复必须包含当前分支，并明确说明：是否提交、是否推送、是否创建/合并 PR、是否部署。
- 默认安全结论应当是：改动只在本地功能分支，线上没有变化。
